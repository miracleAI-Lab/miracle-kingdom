import { expect } from 'chai' //断言模块
import { ethers, upgrades } from 'hardhat' //安全帽模块

describe('TIT 合约测试', () => {
    // 测试执行前的钩子函数
    before(async function() {
      
    })

    // 修改score状态测试
  it('上传数据', async () => {
    for (let i = 1; i <= 5; i++) {
      
    }
  })
})